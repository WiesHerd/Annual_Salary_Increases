/**
 * App data state — provided once at app root via AppStateProvider (inside ParametersStateProvider).
 */

export { AppStateProvider, useAppState } from '../context/app-state-context';
export type { AppStateValue } from '../context/app-state-context';
