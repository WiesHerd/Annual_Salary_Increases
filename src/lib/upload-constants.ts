/**
 * Shared constants for import/upload flows (enterprise limits and hints).
 */

export const MAX_UPLOAD_FILE_BYTES = 10 * 1024 * 1024; // 10 MB
export const ACCEPTED_UPLOAD_FORMATS = 'CSV or XLSX';
export const UPLOAD_FORMAT_HINT = `CSV or XLSX · max 10 MB`;
